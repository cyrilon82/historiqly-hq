<section class="hero">
  <div class="hero-glow"></div>

  <div class="hero-content">
    <h1>History, Powered by <span class="gradient-text">AI</span></h1>
    <p class="hero-subtitle">Chat with the past, investigate the mysteries.</p>
    
    <div class="hero-cta">
      <a href="https://blog.historiqly.com" class="cta-primary" target="_blank" rel="noopener">
        Start Exploring
        <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round">
          <path d="M5 12h14M12 5l7 7-7 7"/>
        </svg>
      </a>
      <a href="https://books.historiqly.com" class="cta-secondary" target="_blank" rel="noopener">
        Read the Thrillers
      </a>
    </div>
  </div>
</section>

<style>
  .hero {
    position: relative;
    padding: 11rem 1.5rem 6rem;
    text-align: center;
    overflow: hidden;
  }

  .hero-glow {
    position: absolute;
    top: -250px;
    left: 50%;
    transform: translateX(-50%);
    width: 140%;
    height: 600px;
    background: radial-gradient(ellipse at center, rgba(93, 181, 133, 0.15) 0%, transparent 70%);
    pointer-events: none;
    filter: blur(90px);
    z-index: 0;
    animation: glow-pulse 8s ease-in-out infinite alternate;
  }

  @keyframes glow-pulse {
    0% { opacity: 0.6; transform: translateX(-50%) scale(1); }
    100% { opacity: 0.9; transform: translateX(-50%) scale(1.1); }
  }

  .hero-content {
    position: relative;
    z-index: 10;
    max-width: 900px;
    margin: 0 auto;
    display: flex;
    flex-direction: column;
    align-items: center;
  }

  .hero h1 {
    font-family: 'Playfair Display', Georgia, serif;
    font-size: clamp(3.5rem, 7vw, 5.5rem);
    font-weight: 800;
    letter-spacing: -0.04em;
    line-height: 1.1;
    margin-bottom: 1.5rem;
    color: var(--foreground);
  }

  .gradient-text {
    background: linear-gradient(135deg, var(--primary) 0%, #a7f3d0 50%, var(--primary) 100%);
    background-size: 200% auto;
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
    animation: shimmer 8s linear infinite;
    filter: drop-shadow(0 0 25px rgba(93, 181, 133, 0.5));
  }

  @keyframes shimmer {
    0% { background-position: 0% center; }
    50% { background-position: 100% center; }
    100% { background-position: 0% center; }
  }

  .hero-subtitle {
    font-size: clamp(1.2rem, 2.5vw, 1.5rem);
    color: var(--muted-foreground);
    line-height: 1.6;
    font-weight: 400;
    max-width: 600px;
    margin-bottom: 2.5rem;
  }

  .hero-cta {
    display: flex;
    gap: 1rem;
    align-items: center;
    justify-content: center;
  }

  .cta-primary {
    display: inline-flex;
    align-items: center;
    gap: 0.6rem;
    padding: 1rem 2.25rem;
    background: var(--primary);
    color: var(--background);
    font-weight: 700;
    font-size: 1.05rem;
    border-radius: 9999px; /* Pill shape */
    text-decoration: none;
    transition: all var(--transition-fast);
    box-shadow: 0 4px 20px rgba(93, 181, 133, 0.3);
  }

  .cta-primary:hover {
    background: var(--primary-hover);
    transform: translateY(-2px);
    box-shadow: 0 8px 30px rgba(93, 181, 133, 0.45);
  }

  .cta-secondary {
    display: inline-flex;
    align-items: center;
    padding: 1rem 1.5rem;
    color: var(--muted-foreground);
    font-weight: 600;
    font-size: 1rem;
    text-decoration: none;
    transition: color var(--transition-fast);
  }

  .cta-secondary:hover {
    color: var(--foreground);
  }

  @media (max-width: 768px) {
    .hero {
      padding: 8rem 1.5rem 4rem;
    }
    
    .hero-cta {
      flex-direction: column;
      width: 100%;
      max-width: 300px;
    }

    .cta-primary, .cta-secondary {
      width: 100%;
      justify-content: center;
    }
  }
</style>
