<section class="hero">
  <div class="hero-content">
    <!-- Corner ornaments (same SVGs as card corners) -->
    <svg class="hero-corner top-left" viewBox="0 0 40 40" fill="none" xmlns="http://www.w3.org/2000/svg" aria-hidden="true">
      <path d="M2 38V8C2 4.68629 4.68629 2 8 2H38" stroke="currentColor" stroke-width="1.5" />
      <path d="M2 28V14C2 7.37258 7.37258 2 14 2H28" stroke="currentColor" stroke-width="0.75" opacity="0.5" />
      <circle cx="2" cy="38" r="2" fill="currentColor" />
      <circle cx="38" cy="2" r="2" fill="currentColor" />
      <path d="M8 2L2 8" stroke="currentColor" stroke-width="0.75" opacity="0.6" />
    </svg>
    <svg class="hero-corner top-right" viewBox="0 0 40 40" fill="none" xmlns="http://www.w3.org/2000/svg" aria-hidden="true">
      <path d="M38 38V8C38 4.68629 35.3137 2 32 2H2" stroke="currentColor" stroke-width="1.5" />
      <path d="M38 28V14C38 7.37258 32.6274 2 26 2H12" stroke="currentColor" stroke-width="0.75" opacity="0.5" />
      <circle cx="38" cy="38" r="2" fill="currentColor" />
      <circle cx="2" cy="2" r="2" fill="currentColor" />
      <path d="M32 2L38 8" stroke="currentColor" stroke-width="0.75" opacity="0.6" />
    </svg>
    <svg class="hero-corner bottom-left" viewBox="0 0 40 40" fill="none" xmlns="http://www.w3.org/2000/svg" aria-hidden="true">
      <path d="M2 2V32C2 35.3137 4.68629 38 8 38H38" stroke="currentColor" stroke-width="1.5" />
      <circle cx="2" cy="2" r="2" fill="currentColor" />
      <circle cx="38" cy="38" r="2" fill="currentColor" />
      <path d="M2 32L8 38" stroke="currentColor" stroke-width="0.75" opacity="0.6" />
    </svg>
    <svg class="hero-corner bottom-right" viewBox="0 0 40 40" fill="none" xmlns="http://www.w3.org/2000/svg" aria-hidden="true">
      <path d="M38 2V32C38 35.3137 35.3137 38 32 38H2" stroke="currentColor" stroke-width="1.5" />
      <circle cx="38" cy="2" r="2" fill="currentColor" />
      <circle cx="2" cy="38" r="2" fill="currentColor" />
      <path d="M38 32L32 38" stroke="currentColor" stroke-width="0.75" opacity="0.6" />
    </svg>

    <!-- Badge cartouche -->
    <div class="hero-badge fade-in">
      <svg class="badge-flourish" viewBox="0 0 24 12" fill="none" xmlns="http://www.w3.org/2000/svg" aria-hidden="true">
        <path d="M22 6H8C4.69 6 2 4.21 2 2" stroke="currentColor" stroke-width="0.75" />
        <circle cx="2" cy="2" r="1.5" fill="currentColor" />
        <circle cx="22" cy="6" r="1" fill="currentColor" opacity="0.6" />
      </svg>
      <span class="badge-pulse"></span>
      AI-Powered History Platform
      <svg class="badge-flourish flipped" viewBox="0 0 24 12" fill="none" xmlns="http://www.w3.org/2000/svg" aria-hidden="true">
        <path d="M22 6H8C4.69 6 2 4.21 2 2" stroke="currentColor" stroke-width="0.75" />
        <circle cx="2" cy="2" r="1.5" fill="currentColor" />
        <circle cx="22" cy="6" r="1" fill="currentColor" opacity="0.6" />
      </svg>
    </div>

    <!-- Headline with decorative rule -->
    <div class="headline-frame fade-in" style="transition-delay: 100ms">
      <div class="headline-rule">
        <svg class="rule-star" viewBox="0 0 16 16" fill="currentColor" aria-hidden="true">
          <path d="M8 2L9.5 6.5L14 8L9.5 9.5L8 14L6.5 9.5L2 8L6.5 6.5Z" />
        </svg>
        <span class="rule-line"></span>
        <svg class="rule-diamond" viewBox="0 0 12 12" fill="currentColor" aria-hidden="true">
          <path d="M6 1L8.5 6L6 11L3.5 6Z" />
        </svg>
        <span class="rule-line"></span>
        <svg class="rule-star" viewBox="0 0 16 16" fill="currentColor" aria-hidden="true">
          <path d="M8 2L9.5 6.5L14 8L9.5 9.5L8 14L6.5 9.5L2 8L6.5 6.5Z" />
        </svg>
      </div>
      <h1>
        History, Powered by&nbsp;<span class="ai-shimmer">AI</span>
      </h1>
    </div>

    <!-- Subtitle -->
    <p class="hero-sub fade-in" style="transition-delay: 200ms">
      Chat with the past, investigate the mysteries.
    </p>

    <!-- Star divider -->
    <div class="hero-divider fade-in" style="transition-delay: 250ms">
      <span class="hero-divider-line"></span>
      <svg class="hero-divider-glyph" viewBox="0 0 16 16" fill="currentColor" aria-hidden="true">
        <path d="M8 2L9.5 6.5L14 8L9.5 9.5L8 14L6.5 9.5L2 8L6.5 6.5Z" />
      </svg>
      <span class="hero-divider-line"></span>
    </div>

    <!-- CTAs -->
    <div class="hero-actions fade-in" style="transition-delay: 320ms">
      <a href="https://app.historiqly.com/" class="btn-primary" target="_blank" rel="noopener">
        Chat with History
        <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round">
          <path d="M5 12h14M12 5l7 7-7 7"/>
        </svg>
      </a>
      <a href="https://books.historiqly.com" class="btn-ghost" target="_blank" rel="noopener">
        Read the Books
        <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
          <path d="M5 12h14M12 5l7 7-7 7"/>
        </svg>
      </a>
    </div>
  </div>
</section>

<style>
  .hero {
    padding: 1.5rem 1.5rem 0;
    text-align: center;
    display: flex;
    justify-content: center;
    align-items: center;
    min-height: 28vh;
  }

  .hero-content {
    position: relative;
    max-width: 800px;
    margin: 0 auto;
    display: flex;
    flex-direction: column;
    align-items: center;
    padding: 2rem 2.5rem;
  }

  /* Dark radial backdrop — dims the map behind hero text */
  .hero-content::before {
    content: '';
    position: absolute;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    width: 140%;
    height: 170%;
    background: radial-gradient(
      ellipse at center,
      rgba(0,0,0,0.72) 0%,
      rgba(0,0,0,0.45) 42%,
      transparent 72%
    );
    pointer-events: none;
    z-index: -1;
  }

  /* Ultra-faint connecting border between corners */
  .hero-content::after {
    content: '';
    position: absolute;
    inset: 10px;
    border: 1px solid rgba(227, 171, 113, 0.06);
    border-radius: 2px;
    pointer-events: none;
    z-index: 0;
  }

  /* ── Hero Corner Ornaments ── */
  .hero-corner {
    position: absolute;
    width: 48px;
    height: 48px;
    color: rgba(227, 171, 113, 0.2);
    z-index: 1;
    pointer-events: none;
    filter: drop-shadow(0 1px 3px rgba(0, 0, 0, 0.5));
  }

  .hero-corner.top-left     { top: 0; left: 0; }
  .hero-corner.top-right    { top: 0; right: 0; }
  .hero-corner.bottom-left  { bottom: 0; left: 0; }
  .hero-corner.bottom-right { bottom: 0; right: 0; }

  /* ── Badge Cartouche ── */
  .hero-badge {
    display: inline-flex;
    align-items: center;
    gap: 0.55rem;
    padding: 0.4rem 1.1rem;
    background: rgba(227, 171, 113, 0.04);
    border: 1px solid rgba(227, 171, 113, 0.28);
    border-radius: 3px;
    font-family: 'Space Grotesk', system-ui, sans-serif;
    font-size: 0.65rem;
    font-weight: 600;
    letter-spacing: 0.08em;
    text-transform: uppercase;
    color: var(--gold);
    margin-bottom: 1rem;
    backdrop-filter: blur(10px);
    box-shadow:
      inset 0 0 0 3px rgba(12, 10, 6, 0.9),
      inset 0 0 0 4px rgba(227, 171, 113, 0.12);
  }

  .badge-flourish {
    width: 22px;
    height: 10px;
    color: rgba(227, 171, 113, 0.4);
    flex-shrink: 0;
  }

  .badge-flourish.flipped {
    transform: scaleX(-1);
  }

  .badge-pulse {
    width: 6px;
    height: 6px;
    border-radius: 50%;
    background: var(--primary);
    box-shadow: 0 0 8px var(--primary-glow);
    animation: glow-pulse 2s ease-in-out infinite;
  }

  @keyframes glow-pulse {
    0%, 100% { opacity: 0.4; box-shadow: 0 0 4px var(--primary-glow); }
    50% { opacity: 1; box-shadow: 0 0 12px var(--primary-glow); }
  }

  /* ── Headline Frame ── */
  .headline-frame {
    display: flex;
    flex-direction: column;
    align-items: center;
    width: 100%;
    max-width: 700px;
  }

  .headline-rule {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    width: 80%;
    max-width: 420px;
    margin-bottom: 0.6rem;
  }

  .rule-line {
    flex: 1;
    height: 1px;
    background: linear-gradient(
      90deg,
      transparent,
      rgba(227, 171, 113, 0.35) 30%,
      rgba(227, 171, 113, 0.35) 70%,
      transparent
    );
  }

  .rule-star {
    width: 10px;
    height: 10px;
    color: rgba(227, 171, 113, 0.45);
    flex-shrink: 0;
  }

  .rule-diamond {
    width: 8px;
    height: 8px;
    color: rgba(227, 171, 113, 0.35);
    flex-shrink: 0;
  }

  /* ── Headline ── */
  .hero h1 {
    font-family: 'Playfair Display', Georgia, serif;
    font-size: clamp(3.2rem, 8vw, 6rem);
    font-weight: 800;
    letter-spacing: -0.025em;
    line-height: 1.06;
    margin-bottom: 0.5rem;
    color: var(--foreground);
    text-shadow: 0 4px 20px rgba(0,0,0,0.8);
  }

  /* ── Luminous gold shimmer on AI ── */
  .ai-shimmer {
    display: inline-block;
    background: linear-gradient(
      90deg,
      #e3ab71 0%,
      #fff8ee 25%,
      #f3e3c6 50%,
      #fff8ee 75%,
      #e3ab71 100%
    );
    background-size: 200% auto;
    -webkit-background-clip: text;
    background-clip: text;
    color: transparent;
    text-shadow: none;
    animation: shimmer 3s ease-in-out infinite;
    filter: drop-shadow(0 4px 20px rgba(227, 171, 113, 0.55));
  }

  @keyframes shimmer {
    0% { background-position: 200% center; }
    100% { background-position: -200% center; }
  }

  /* ── Subtitle ── */
  .hero-sub {
    font-family: 'Playfair Display', Georgia, serif;
    font-size: clamp(1.05rem, 2.5vw, 1.4rem);
    color: rgba(237, 233, 221, 0.8);
    line-height: 1.7;
    font-weight: 400;
    font-style: italic;
    margin-bottom: 0.75rem;
    text-shadow: 0 2px 8px rgba(0,0,0,0.65);
    letter-spacing: 0.02em;
  }

  /* ── Hero Divider ── */
  .hero-divider {
    display: flex;
    align-items: center;
    gap: 0.75rem;
    width: 200px;
    margin-bottom: 1.75rem;
  }

  .hero-divider-line {
    flex: 1;
    height: 1px;
    background: linear-gradient(90deg, transparent, rgba(227, 171, 113, 0.35));
  }

  .hero-divider-line:last-child {
    background: linear-gradient(90deg, rgba(227, 171, 113, 0.35), transparent);
  }

  .hero-divider-glyph {
    width: 14px;
    height: 14px;
    color: rgba(227, 171, 113, 0.45);
    flex-shrink: 0;
    animation: slow-rotate 20s linear infinite;
  }

  @keyframes slow-rotate {
    from { transform: rotate(0deg); }
    to { transform: rotate(360deg); }
  }

  /* ── CTAs ── */
  .hero-actions {
    display: flex;
    gap: 1rem;
    align-items: center;
    flex-wrap: wrap;
    justify-content: center;
  }

  /* ── Primary Button ── */
  .btn-primary {
    display: inline-flex;
    align-items: center;
    gap: 0.75rem;
    padding: 0.85rem 2.2rem;
    background: linear-gradient(180deg, #6a5848 0%, #4e4235 100%);
    color: #fff;
    font-family: 'Space Grotesk', system-ui, sans-serif;
    font-weight: 700;
    font-size: 0.9rem;
    letter-spacing: 0.06em;
    text-transform: uppercase;
    text-decoration: none;
    border-radius: 3px;
    border: 1px solid rgba(227, 171, 113, 0.45);
    transition: all 0.3s cubic-bezier(0.25, 0.8, 0.25, 1);
    box-shadow:
      inset 0 0 0 3px rgba(78, 66, 53, 0.6),
      inset 0 0 0 4px rgba(227, 171, 113, 0.18),
      0 0 28px rgba(227, 171, 113, 0.25),
      0 14px 30px rgba(0, 0, 0, 0.55);
    position: relative;
    overflow: hidden;
  }

  .btn-primary::before {
    content: '';
    position: absolute;
    top: 0; left: 0; right: 0;
    height: 50%;
    background: linear-gradient(180deg, rgba(255,255,255,0.12) 0%, transparent 100%);
    pointer-events: none;
  }

  /* Corner dots */
  .btn-primary::after {
    content: '';
    position: absolute;
    inset: 4px;
    background:
      radial-gradient(circle at 0% 0%, rgba(227, 171, 113, 0.5) 1.5px, transparent 1.5px),
      radial-gradient(circle at 100% 0%, rgba(227, 171, 113, 0.5) 1.5px, transparent 1.5px),
      radial-gradient(circle at 0% 100%, rgba(227, 171, 113, 0.5) 1.5px, transparent 1.5px),
      radial-gradient(circle at 100% 100%, rgba(227, 171, 113, 0.5) 1.5px, transparent 1.5px);
    pointer-events: none;
    z-index: 1;
  }

  .btn-primary:hover {
    transform: translateY(-2px);
    border-color: rgba(227, 171, 113, 0.65);
    box-shadow:
      inset 0 0 0 3px rgba(78, 66, 53, 0.5),
      inset 0 0 0 4px rgba(227, 171, 113, 0.3),
      0 0 40px rgba(227, 171, 113, 0.35),
      0 14px 30px rgba(0, 0, 0, 0.55);
    color: #fff;
  }

  .btn-primary svg {
    transition: transform 0.3s cubic-bezier(0.25, 0.8, 0.25, 1);
  }

  .btn-primary:hover svg {
    transform: translateX(4px);
  }

  /* ── Ghost Button ── */
  .btn-ghost {
    display: inline-flex;
    align-items: center;
    gap: 0.75rem;
    padding: 0.85rem 2rem;
    color: var(--gold);
    font-family: 'Space Grotesk', system-ui, sans-serif;
    font-weight: 600;
    font-size: 0.9rem;
    letter-spacing: 0.06em;
    text-transform: uppercase;
    text-decoration: none;
    border: 1px solid rgba(227, 171, 113, 0.25);
    background: rgba(40, 38, 31, 0.65);
    backdrop-filter: blur(8px);
    border-radius: 3px;
    transition: all 0.3s cubic-bezier(0.25, 0.8, 0.25, 1);
    box-shadow:
      inset 0 0 0 3px rgba(12, 10, 6, 0.8),
      inset 0 0 0 4px rgba(227, 171, 113, 0.10),
      0 8px 20px rgba(0, 0, 0, 0.35);
    position: relative;
  }

  /* Corner dots */
  .btn-ghost::after {
    content: '';
    position: absolute;
    inset: 4px;
    background:
      radial-gradient(circle at 0% 0%, rgba(227, 171, 113, 0.35) 1.5px, transparent 1.5px),
      radial-gradient(circle at 100% 0%, rgba(227, 171, 113, 0.35) 1.5px, transparent 1.5px),
      radial-gradient(circle at 0% 100%, rgba(227, 171, 113, 0.35) 1.5px, transparent 1.5px),
      radial-gradient(circle at 100% 100%, rgba(227, 171, 113, 0.35) 1.5px, transparent 1.5px);
    pointer-events: none;
  }

  .btn-ghost:hover {
    background: rgba(50, 47, 38, 0.85);
    border-color: rgba(227, 171, 113, 0.45);
    transform: translateY(-2px);
    color: var(--gold-light);
    box-shadow:
      inset 0 0 0 3px rgba(12, 10, 6, 0.7),
      inset 0 0 0 4px rgba(227, 171, 113, 0.20),
      0 12px 28px rgba(0, 0, 0, 0.45),
      0 0 20px rgba(227, 171, 113, 0.08);
  }

  .btn-ghost svg {
    transition: transform 0.3s cubic-bezier(0.25, 0.8, 0.25, 1);
  }

  .btn-ghost:hover svg {
    transform: translateX(4px);
  }

  /* ── Mobile ── */
  @media (max-width: 768px) {
    .hero {
      padding: 0.75rem 1.25rem 1rem;
      min-height: auto;
    }

    .hero-content {
      padding: 1.25rem 1rem;
    }

    .hero-corner {
      width: 28px;
      height: 28px;
      color: rgba(227, 171, 113, 0.12);
    }

    .hero-content::after {
      display: none;
    }

    .hero-badge {
      font-size: 0.55rem;
      margin-bottom: 0.6rem;
      padding: 0.3rem 0.7rem;
      box-shadow:
        inset 0 0 0 2px rgba(12, 10, 6, 0.9),
        inset 0 0 0 3px rgba(227, 171, 113, 0.1);
    }

    .badge-flourish {
      width: 16px;
      height: 8px;
    }

    .headline-rule {
      width: 70%;
      max-width: 260px;
      margin-bottom: 0.4rem;
      gap: 0.35rem;
    }

    .rule-star {
      width: 7px;
      height: 7px;
    }

    .rule-diamond {
      width: 5px;
      height: 5px;
    }

    .hero h1 {
      font-size: clamp(2.2rem, 9vw, 3rem);
      margin-bottom: 0.4rem;
    }

    .hero-sub {
      font-size: 0.9rem;
      margin-bottom: 0.5rem;
      color: rgba(237, 233, 221, 0.65);
    }

    .hero-divider {
      width: 140px;
      gap: 0.5rem;
      margin-bottom: 1rem;
    }

    .hero-divider-glyph {
      width: 10px;
      height: 10px;
    }

    .hero-actions {
      flex-direction: row;
      gap: 0.625rem;
    }

    .btn-primary {
      padding: 0.6rem 1.25rem;
      font-size: 0.75rem;
      gap: 0.5rem;
      box-shadow:
        inset 0 0 0 2px rgba(78, 66, 53, 0.6),
        inset 0 0 0 3px rgba(227, 171, 113, 0.15),
        0 0 16px rgba(227, 171, 113, 0.2),
        0 8px 20px rgba(0, 0, 0, 0.4);
    }

    .btn-primary::after,
    .btn-ghost::after {
      inset: 3px;
      background:
        radial-gradient(circle at 0% 0%, rgba(227,171,113,0.4) 1px, transparent 1px),
        radial-gradient(circle at 100% 0%, rgba(227,171,113,0.4) 1px, transparent 1px),
        radial-gradient(circle at 0% 100%, rgba(227,171,113,0.4) 1px, transparent 1px),
        radial-gradient(circle at 100% 100%, rgba(227,171,113,0.4) 1px, transparent 1px);
    }

    .btn-primary svg {
      width: 14px;
      height: 14px;
    }

    .btn-ghost {
      padding: 0.55rem 1.1rem;
      font-size: 0.7rem;
      gap: 0.5rem;
      box-shadow:
        inset 0 0 0 2px rgba(12, 10, 6, 0.8),
        inset 0 0 0 3px rgba(227, 171, 113, 0.08),
        0 6px 14px rgba(0, 0, 0, 0.3);
    }

    .btn-ghost svg {
      width: 12px;
      height: 12px;
    }
  }
</style>
